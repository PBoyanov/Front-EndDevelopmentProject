<div class="container">
    {{!--<h2>Materials</h2>
    <form class="search">
       <div class="form-group">
            <label for="title">Title: </label>
            <input type="text" class="form-control" id="title" placeholder="title">
            <button class="btn btn-info" id="title-btn">Search by title</button>
        </div>
        <div class="form-group">
            <label for="description">Description: </label>
            <input type="text" class="form-control" id="description" placeholder="title">
            <button class="btn btn-info" id="description-btn">Search by description</button>
        </div>
        <div class="form-group">
            <label for="user">User: </label>
            <input type="text" class="form-control" id="user" placeholder="title">
            <button class="btn btn-info" id="user-btn">Search by user</button>
        </div>
    </form>
    <ul id="materials-list" class="list-style-none">
        {{#materials}}
            <li class="material-info" data-id="{{id}}">
                <h3><a href="#/materials/{{id}}">{{title}}</a></h3>
                <img src="{{img}}" alt="" class="img-responsive smaller-image">
                <div class="material-description">{{description}}</div>
                <div class="smaller-font">Created on {{createdOn}}</div>
                <div>Created by <a href="#/profiles/{{user.username}}">{{user.username}}</a></div>
            </li>
        {{/materials}}
    </ul>--}}
    Hello. It`s me!
</div>